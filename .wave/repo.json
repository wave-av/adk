{
  "name": "adk",
  "kind": "library",
  "domain": "agents",
  "purpose": "TypeScript SDK (@wave-av/adk on npm) for building AI agents that interact with WAVE's live video infrastructure: 5 ready-made agent templates, an MCP toolkit exposing 10 tools, a production agent runtime (health/heartbeat/graceful-shutdown), and adapters for Mastra, LangGraph, LiveKit, and Kernel.sh.",
  "visibility": "public",
  "primaryLanguage": "TypeScript",
  "topics": ["agents", "sdk", "video", "streaming", "mcp", "ai", "developer-kit", "typescript"],
  "capabilities": [
    { "id": "agent-templates", "does": "5 ready-made agent template classes extending WaveAgent: StreamMonitorAgent, AutoProducerAgent, ClipFactoryAgent, ModerationAgent, CaptionAgent.", "status": "ga" },
    { "id": "mcp-toolkit", "does": "AgentToolkit.toMCPTools() exposes 10 MCP tool definitions (wave_create_stream, wave_monitor_stream, wave_create_clip, wave_switch_camera, wave_show_graphic, wave_moderate_chat, wave_start_captions, wave_analyze_quality, wave_mark_highlight, wave_control_camera).", "status": "ga" },
    { "id": "agent-runtime", "does": "AgentRuntime provides an HTTP health server (/health, /ready, /metrics), a 30s heartbeat loop, structured JSON logging, and graceful SIGTERM/SIGINT shutdown.", "status": "ga" },
    { "id": "framework-adapters", "does": "Adapter functions for Mastra (createMastraTools), LangGraph (createLangGraphTools), LiveKit (createLiveKitWaveTools), and Kernel.sh (createKernelTools).", "status": "ga" },
    { "id": "subpath-exports", "does": "README documents 6 tree-shakeable subpath imports (root, /tools, /agents, /adapters, /templates, /types), but package.json's exports map only declares the root \".\" entry and dist/ contains only index.js/index.cjs — the subpaths are not actually built or resolvable.", "status": "planned" },
    { "id": "cli-binary", "does": "package.json declares a wave-adk CLI bin pointing at ./dist/cli/index.mjs, but dist/ (tracked in git) has no cli/ output — the CLI has never been built.", "status": "planned" }
  ],
  "claims": [
    { "id": "npm-version-1-0-2", "text": "Published on npm as @wave-av/adk, currently at version 1.0.2.", "resolver": { "type": "grep", "target": "package.json", "expect": "\"version\": \"1.0.2\"" } },
    { "id": "license-apache-2-0", "text": "License is Apache-2.0.", "resolver": { "type": "grep", "target": "package.json", "expect": "\"license\": \"Apache-2.0\"" } },
    { "id": "lifecycle-beta", "text": "capabilities.json declares lifecycle beta.", "resolver": { "type": "grep", "target": "capabilities.json", "expect": "\"lifecycle\": \"beta\"" } },
    { "id": "dual-esm-cjs-build", "text": "Built as dual esm/cjs output via tsup.", "resolver": { "type": "grep", "target": "package.json", "expect": "esm,cjs" } },
    { "id": "ten-mcp-tools", "text": "AgentToolkit exposes MCP tool definitions via toMCPTools().", "resolver": { "type": "grep", "target": "src/tools/AgentToolkit.ts", "expect": "toMCPTools" } },
    { "id": "heartbeat-30s", "text": "AgentRuntime defaults heartbeatIntervalMs to 30 seconds.", "resolver": { "type": "grep", "target": "src/agents/AgentRuntime.ts", "expect": "heartbeatIntervalMs ?? 30_000" } },
    { "id": "graceful-sigterm", "text": "AgentRuntime handles SIGTERM for graceful shutdown.", "resolver": { "type": "grep", "target": "src/agents/AgentRuntime.ts", "expect": "SIGTERM" } },
    { "id": "cli-bin-declared", "text": "package.json declares a wave-adk CLI binary at ./dist/cli/index.mjs (not yet present in the built dist/).", "resolver": { "type": "grep", "target": "package.json", "expect": "./dist/cli/index.mjs" } }
  ],
  "endpoints": [
    { "method": "GET", "path": "/health", "does": "Liveness probe returning { status, uptime } while an AgentRuntime is running." },
    { "method": "GET", "path": "/ready", "does": "Readiness probe returning { ready: true } while an AgentRuntime is running." },
    { "method": "GET", "path": "/metrics", "does": "Usage stats returning { totalCalls, totalDurationMs } while an AgentRuntime is running." }
  ],
  "install": [
    { "manager": "npm", "cmd": "npm install @wave-av/adk" }
  ],
  "quickstart": {
    "lang": "typescript",
    "code": "import { StreamMonitorAgent } from '@wave-av/adk';\n\nconst monitor = new StreamMonitorAgent({\n  apiKey: process.env.WAVE_AGENT_KEY,\n  agentName: 'my-quality-monitor',\n  streamIds: ['stream_abc123'],\n  autoRemediate: true,\n  onQualityDrop: async (alert) => {\n    console.log(`Quality drop on ${alert.streamId}: ${alert.metric}`);\n  },\n});\n\nawait monitor.start();"
  },
  "mcp": { "tool": "AgentToolkit.toMCPTools", "transport": "stdio" },
  "links": {
    "npm": "https://www.npmjs.com/package/@wave-av/adk",
    "homepage": "https://wave.online/developers/adk"
  },
  "sections": [
    {
      "kind": "prose",
      "heading": "Status",
      "body": "Beta. The core SDK is real and implemented: 5 agent templates, the 10-tool MCP toolkit, AgentRuntime's health/heartbeat/shutdown lifecycle, and the four framework adapters all exist as working source in src/ and are covered by tests. Two README-advertised surfaces are not yet delivered, however: the 6 tree-shakeable subpath exports (only the root \".\" is declared in package.json's exports map and built into dist/), and the wave-adk CLI binary (declared in package.json's bin field but dist/cli/ has never been built). Both are marked planned rather than ga to keep this SSOT honest. The README's '$19/month usage-based pricing' claim is marketing copy with no billing config in this repo to verify it against, so it is omitted from claims here."
    }
  ]
}
